# 财务分析工作流程

本文件与同目录的 `SKILL.md` 配套，说明本专项从启动到交付的两条路径。分析方法和专项验收仍以主 Skill 为准。

## 1. 选择启动路径

- **已安装 `hehe-industry-researcher`：** 即使用户只提出本专项问题，也先由该路由按“单独专项研究”组织。收到任务卡和材料索引后直接执行，不重新启动路由或开题。
- **未安装 `hehe-industry-researcher`：** 用户点名本专项或提出与其职责相符的问题时，直接单独启动，不把其他 Skill 作为启动条件。

## 2. 接任务或确认范围

- **由路由调用：** 先读任务卡中的研究范围、专项问题、已有材料、待补字段和预期交付，再按材料索引读取原文。已经说明的信息不重复询问；仍有会改变分析结果的歧义时，交回路由补充确认。
- **单独启动：** 先读取用户描述和已有材料，在对话中确认会影响结果的对象与范围。用户已经说明的内容不重复询问；Agent 的推断不能冒充用户确认，不要求另外制作开题文件。

公司对象、报告期或分析问题存在重大歧义时才向用户追问；其余财务范围和口径在分析中查找确认。搜索和材料核对后，按主 Skill 确定任务及材料路径，交付名称使用交付模板。

## 信息搜索

### 有 `hehe-industry-researcher` 时

读取 `hehe-industry-researcher` 安装目录中的 `references/信息搜索流程.md`，按其中的搜索方向、能力调用、材料审核和保存安排执行。单独调用本专项时，也由该路由按“单独专项研究”组织搜索；已经由路由交来材料时，沿用材料及审核记录，发现缺口再定向补搜，完成后继续本专项分析。

### 没有 `hehe-industry-researcher` 时

先查看用户已有材料；没有材料就搜索，有材料时确认是否需要补搜。已确认的搜索安排不重复询问；用户明确不联网时，只分析现有材料。

1. **设计关键词。** 根据上文所列资料需求，有 `sou-cha-ling`（搜查令 Skill）就调用对应专项的行业或企业关键词组件；没有则自行组合对象名称、业务/指标、地域和时间，并扩展中英文名、简称、旧称、同义词及上下位概念。
2. **选择搜索能力。** 检查当前实际可调用的能力，按下表选择；示例包括但不限于所列工具，不要求全部使用。

| 能力 | 示例 | 用法 |
|---|---|---|
| 网页搜索与浏览 | Web Search、WebFetch、内置或已连接浏览器 | 广泛发现来源，再打开具体页面读原文 |
| 信息采集 Skill | `tanzi`、`last30days`、`agent-reach` 等 | 跨平台、批量或社区采集；读取已安装 Skill 的用法后调用 |
| 搜索与数据 MCP/API | Exa、Tavily、Serper 或其他已连接服务 | 补充垂直搜索与结构化数据；付费、登录按用户授权使用 |
| 原始网站与专业数据库 | 政府统计、监管/交易所、公司官网、招投标、行业数据库 | 直接取得数据、披露、政策、交易与执行记录 |
| 财务数据与报表工具 | 交易所结构化数据、XBRL、OpenBB、EdgarTools、FinanceToolkit 或同类可用工具 | 加快多期财报提取、标准化和比率计算；结果必须回到法定披露核对字段映射、期间和单位 |
| 学术论文与预印本 | arXiv 网站或 API | 涉及学术概念、算法、AI 模型或前沿技术时必查；保留标题、作者、摘要、日期和链接，核读关键论文 |
| 网页与文件提取 | URL 正文提取、PDF/Office 解析、OCR | 已知网址或文件时直接读取正文、表格和附件 |

3. **找回原文。** 优先交易所、监管机构、公司财报及附注、审计报告、监管问询和投资者关系原文；非上市企业可使用管理报表、纳税、银行流水、合同和融资材料相互验证。结构化数据库和自动映射只用于提效，新闻与搜索摘要只作线索。
4. **补齐缺口并留来源。** 一个渠道覆盖不足或调用失败时换其他可用能力，不让所有工具重复同一查询。保留标题、来源、日期、链接或文件位置、关键摘录及未取得的材料；按本专项原有交付规则整理进材料表或分析底稿。

### 材料审核与自检

有 `hehe-baojie`（保洁 Skill）时调用它；不可用时至少核对财报原文、主体、期间、合并与分部口径、会计准则、币种与单位、重述、重复和冲突，并在交付前自检三表勾稽、计算血缘、异常变化、披露断层与反例。证据缺口登记到受影响判断。

## 4. 分析与自审

按主 Skill 完成分析与自审。关键缺口先补证；暂时无法解决时，降低或暂停受影响判断，其余分析继续。

补证先检查最接近原值的披露、附注或同版原图，再按具体缺口搜索能区分解释的材料。每次新增检索应有明确待证字段或解释，不重复宽泛搜索。

- 原因已核实，或合理取值范围内结论保持稳定且事项性质不严重时，登记依据后结束本项补证；未知原因仍保留未知。
- 已核对直接来源并完成针对性补查，新增材料没有改变可用证据，且没有具体未查线索时，停止本轮搜索，记录重启条件（如新披露、原表导出或合同资料）。
- 缺口仍可能翻转结论，或涉及资金占用、契约违约等性质严重事项时，暂停受影响判断并列明所需材料。小金额不能单独作为停止理由。差额精度按[公式参考](财务口径、公式与工作底表.md)处理。

## 5. 交付与返修

按 [财务分析交付模板](财务分析交付模板.md) 的输出结构、载体和文件命名规则交付。

- **由路由调用：** 保存到任务卡指定的 `02_专项分析/` 或项目对应位置；交回实际文件清单、使用及新增材料、未解决问题。`hehe-industry-researcher` 二次审核后如退回，修正受影响部分并再次自审后提交。
- **单独启动：** 自审后直接交付，默认保存到当前任务工作文件夹；用户指定位置时按其要求保存，不等待路由审核。

用户明确不落文件时，在对话中完成专项交付。
